// Never ever put these in a public repo.
module.exports = {
    // API keys
    consumer_key: '',
    consumer_secret: '',

    // Below would be one's own personal access tokens
    access_token: '',
    access_token_secret: ''
}